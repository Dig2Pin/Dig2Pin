'use strict';

function _interopDefault(ex) {
  return ex && typeof ex === 'object' && 'default' in ex ? ex['default'] : ex;
}

var dateFns = require('date-fns');

var dayPicker = require('@arch-ui/day-picker');

var React = require('react');

var React__default = _interopDefault(React);

const FORMAT = 'YYYY-MM-DD';

const CalendarDayFilterView = ({
  onChange,
  filter
}) => {
  const [value, setValue] = React.useState(dateFns.format(new Date(), FORMAT));

  const handleSelectedChange = newValue => {
    if (newValue === null) {
      newValue = dateFns.format(new Date(), FORMAT);
    }

    onChange(newValue);
    setValue(newValue);
  };

  if (!filter) return null;
  return React__default.createElement(dayPicker.TextDayPicker, {
    date: value,
    onChange: handleSelectedChange
  });
};

exports.default = CalendarDayFilterView;
