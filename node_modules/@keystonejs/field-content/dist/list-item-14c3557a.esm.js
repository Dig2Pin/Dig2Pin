import React__default from 'react';
let type = 'list-item';

function Node({
  attributes,
  children
}) {
  return React__default.createElement("li", attributes, children);
}

export { Node, type };
