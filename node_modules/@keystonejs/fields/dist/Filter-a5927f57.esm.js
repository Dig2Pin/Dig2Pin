import React__default from 'react';
import { RadioGroup, Radio } from '@arch-ui/filters';

const CheckboxFilterView = ({
  onChange,
  filter,
  value
}) => {
  if (!filter) return null;
  return React__default.createElement(RadioGroup, {
    onChange: onChange,
    value: value
  }, React__default.createElement(Radio, {
    value: "true"
  }, "Checked"), React__default.createElement(Radio, {
    value: "false"
  }, "Unchecked"), React__default.createElement(Radio, {
    value: "null"
  }, "Not set"));
};

export default CheckboxFilterView;
