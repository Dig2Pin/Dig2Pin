import { FieldContainer, FieldLabel, FieldDescription, FieldInput } from '@arch-ui/fields';
import React__default from 'react';
import { Input } from '@arch-ui/input';

const IntegerField = ({
  onChange,
  autoFocus,
  field,
  value,
  errors
}) => {
  const handleChange = event => {
    const value = event.target.value;
    onChange(value.replace(/\D/g, ''));
  };

  const valueToString = value => {
    // Make the value a string to keep react happy.
    if (typeof value === 'string') {
      return value;
    } else if (typeof value === 'number') {
      return String(value);
    } else {
      // If it is neither string nor number then it must be empty.
      return '';
    }
  };

  const htmlID = `ks-input-${field.path}`;
  return React__default.createElement(FieldContainer, null, React__default.createElement(FieldLabel, {
    htmlFor: htmlID,
    field: field,
    errors: errors
  }), React__default.createElement(FieldDescription, {
    text: field.adminDoc
  }), React__default.createElement(FieldInput, null, React__default.createElement(Input, {
    autoComplete: "off",
    autoFocus: autoFocus,
    type: "text",
    value: valueToString(value),
    onChange: handleChange,
    id: htmlID
  })));
};

export default IntegerField;
